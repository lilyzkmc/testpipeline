trigger:
- main  # or any branch you want

pool:
  name: Pool

jobs:
- job: FailJob
  steps:
    - script: |
        echo "This will fail now"
        exit 1
      displayName: "Intentional failure step"
